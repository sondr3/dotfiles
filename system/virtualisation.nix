{
  # Enable Docker
  virtualisation = {
    docker = {
      enable = true;
      enableOnBoot = false;
    };

    # virtualbox = {
    #   host.enable = true;
    #   host.enableExtensionPack = true;
    #   guest.enable = true;
    # };
  };
}
